# URL de base de l'API backend okoume_portal. En dev, si le backend tourne en local :
# EXPO_PUBLIC_API_URL=http://192.168.x.x:3000 (l'IP de votre machine, pas localhost,
# pour être joignable depuis un appareil/simulateur physique).
EXPO_PUBLIC_API_URL=http://localhost:3000